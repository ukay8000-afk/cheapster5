# Copy this file to ".env" and fill in your real values.
# NEVER commit the real ".env" file to GitHub — it's already excluded in .gitignore.

CUELINKS_API_KEY=your_cuelinks_api_key_here
CUELINKS_CHANNEL_ID=320109
DB_PATH=cheapster.db
